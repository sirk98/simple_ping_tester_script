# simple ping tester script
Run ping and test some target

## This is a simple script that allows to test latency or network.
### Run this script as root and enjoy.

```bash
sudo python3 /tmp/pingtester.py
```